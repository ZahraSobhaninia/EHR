logging:
  level: INFO
  path: ./outputs/logs

  

paths:

#  features: 'researcher_data:Zahra/032026/CoreBehrt_OOT/MDPS/0_CreateData/features'
#  tokenized: 'researcher_data:Zahra/032026/CoreBehrt_OOT/MDPS/0_CreateData/tokenized'
  #initial_pids: researcher_data:Zahra/CoreBehrt_OOT/MDP/0_CreateData/tokenized/pids_train.pt' # Initial patients to select from [list of pids] (optional)
#  outcomes: 'researcher_data:Zahra/032026/CoreBehrt_OOT/CreateOutcome_Finetune/Mortality'
  outcome: 'outcome.csv' # Outcome
  
#  cohort: "researcher_data:Zahra/032026/CoreBehrt_OOT/MDPS/2_Finetune/Mortality/Data/SelectCohort" # path to cohort directory   #MDP_withoutP

  ## OUTPUTS  
  prepared_data: "researcher_data:Zahra/032026/CoreBehrt_OOT/MDP/2_Finetune/Mortality/Data/Finetunedata" # Save model/outputs to this folder
  #runs: ./outputs/pretraining # Use for generating a new model folder


data:
  type: "finetune"
  truncation_len: 512
  min_len: 2 # 0 by default

outcome:
  # we will convert outcomes to binary based on whether at least one outcome is in the follow up window
  n_hours_censoring: 0 # censor time after index date (negative means before)
  n_hours_start_follow_up: 0 # start follow up (considering outcomes) time after index date (negative means before)
  n_hours_end_follow_up: 720 # end follow up (considering outcomes) time after index date (negative means before)

#concept_pattern_hours_delay:
  # Diagnosis concepts (matching strings starting with "D")
  #"^D": 72 # censoring delayed by 72 hours
